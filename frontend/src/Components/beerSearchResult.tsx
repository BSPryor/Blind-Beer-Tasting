import IComponent from "../Interfaces/component";

const BeerSearchResult: React.FunctionComponent<IComponent> = (props) => {
  return (
    <div>
      <input></input>
    </div>
  );
};

export default BeerSearchResult;
